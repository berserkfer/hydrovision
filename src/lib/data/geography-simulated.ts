/**
 * @deprecated Use `@/lib/repositories/geography.repository` — re-export de compatibilidad Fase 3.0
 */
export {
  GEOGRAPHIC_HIERARCHY,
  DEFAULT_MAP_FILTERS,
  findRiverContext,
  getSummariesForRiver,
  getFilteredSummaries,
  resolveMapView,
  getStationDetailById,
  buildDashboardTitle,
} from "@/lib/repositories/geography.repository";
